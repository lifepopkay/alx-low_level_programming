#include <stdio.h>

/**
 * main - this print the name of program
 * @argc: argument count
 * @argv: array of character
 *
 * Return: Always 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
